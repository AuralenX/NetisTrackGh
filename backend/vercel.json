{
  "version": 2,
  "buildCommand": "npm install",
  "installCommand": "npm ci",
  "framework": "express",
  "functions": {
    "api/**/*.js": {
      "memory": 512,
      "maxDuration": 30,
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/:path(.*)",
      "destination": "/api/server.js"
    }
  ]
}
